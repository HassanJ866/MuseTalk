# torch/torchvision are NOT listed here — Colab pre-installs a compatible build.
# On a bare machine, install manually before running setup.sh:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# HuggingFace ecosystem
transformers>=4.39.2
diffusers
huggingface_hub
accelerate
safetensors
peft>=0.17.0

# OpenMMLab — installed via mim in setup.sh (listed for reference only)
# mmengine
# mmcv==2.1.0
# mmdet==3.1.0
# mmpose==1.1.0

# Vision & video
opencv-python>=4.9.0
Pillow
moviepy
imageio[ffmpeg]
albumentations==1.4.20

# Audio
openai-whisper

# Scientific computing
numpy<2.0
scipy

# Web UI
gradio

# Utilities
pyyaml
tqdm
gdown
requests
