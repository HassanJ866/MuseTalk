# torch/torchvision are NOT pinned here — Colab pre-installs a compatible version.
# If running outside Colab on a bare machine, install manually first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# HuggingFace ecosystem
transformers>=4.39.2
diffusers
huggingface_hub
accelerate
safetensors
peft==0.10.0

# OpenMMLab (installed via mim in setup.sh, listed here for reference)
# mmengine==0.10.5
# mmcv==2.0.1
# mmdet==3.1.0
# mmpose==1.1.0

# Vision & video
opencv-python>=4.9.0
Pillow
moviepy
imageio[ffmpeg]
albumentations==1.4.20

# Audio
openai-whisper

# Scientific computing
numpy<2.0
scipy

# Web UI
gradio

# Utilities
pyyaml
tqdm
gdown
requests
